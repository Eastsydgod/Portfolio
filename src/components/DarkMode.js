// import React, { useState } from "react";
// import LightModeIcon from "@mui/icons-material/LightMode";
// import DarkModeIcon from "@mui/icons-material/DarkMode";

// const DarkMode = () => {
//   const [isDarkMode, setIsDarkMode] = useState(false);
//   const toggleDarkMode = () => {
//     setIsDarkMode((prevMode) => !prevMode);
//     // Add logic to switch the theme or update the CSS classes accordingly
//     // For example, you can update the body class or use a CSS-in-JS library to update  the styles dynamically
//   };
//   return (
//     <div onClick={toggleDarkMode} className="toggle">
//       {isDarkMode ? (
//         <LightModeIcon className="icon" />
//       ) : (
//         <DarkModeIcon className="icon" />
//       )}
//     </div>
//   );
// };

// export default DarkMode;
